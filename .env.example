# TMDB API key (https://www.themoviedb.org/settings/api — free for personal use).
# When set, build-time fetches enrich each movie with poster / director / year / genres / overview.
# When unset, the site builds with whatever data is in src/lib/data/movies.ts (no posters).
TMDB_API_KEY=
